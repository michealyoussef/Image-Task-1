using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace WindowsFormsApplication1
{
    class Generatemask
    {
        public double[,] mask;

        public double[,] Generatingmean(int w, int h)
        {
            mask = new double[w, h];

            return mask;
        }
        public double[,] Generatingmedian(int w, int h)
        {
            mask = new double[w, h];

            return mask;
        }
        public double[,] Gaussian_Filter_Option1(int w, int h)
        {
            mask = new double[w, h];

            return mask;
        }
        public double[,] Gaussian_Filter_Option2(int w, int h)
        {
            mask = new double[w, h];

            return mask;
        }
    }
}
